apiVersion: v1
kind: Service
metadata:
  name: {{ include "datasciencepipelines.fullname" . }}
  labels:
    {{- include "datasciencepipelines.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datasciencepipelines.selectorLabels" . | nindent 4 }}
