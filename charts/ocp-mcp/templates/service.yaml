apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocp-mcp.fullname" . }}
  labels:
    {{- include "ocp-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocp-mcp.selectorLabels" . | nindent 4 }}
