apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llamastack-playground.fullname" . }}-test-connection"
  labels:
    {{- include "llamastack-playground.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llamastack-playground.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
