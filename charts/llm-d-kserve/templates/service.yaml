apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-d-kserve.fullname" . }}
  labels:
    {{- include "llm-d-kserve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-d-kserve.selectorLabels" . | nindent 4 }}
